From 9156fae5de989c4b0cbb207bd47941f6ffce5ce1 Mon Sep 17 00:00:00 2001
From: Mesh-LLM CI <ci@mesh-llm.local>
Date: Fri, 24 Jul 2026 19:02:35 +1000
Subject: [PATCH 11/12] skippy: filter Inkling stage memory by layer range

Assisted-by: goose
---
 include/skippy/common.h |  2 +-
 src/llama-model.cpp     | 11 +++++++++--
 src/skippy.cpp          |  4 ++--
 3 files changed, 12 insertions(+), 5 deletions(-)

diff --git a/include/skippy/common.h b/include/skippy/common.h
index 8c283047b..11f06bf3e 100644
--- a/include/skippy/common.h
+++ b/include/skippy/common.h
@@ -26,7 +26,7 @@ extern "C" {
 
 #define SKIPPY_ABI_VERSION_MAJOR 0
 #define SKIPPY_ABI_VERSION_MINOR 1
-#define SKIPPY_ABI_VERSION_PATCH 32
+#define SKIPPY_ABI_VERSION_PATCH 33
 
 enum skippy_feature {
     SKIPPY_FEATURE_RUNTIME_SLICE          = 1 << 0,
diff --git a/src/llama-model.cpp b/src/llama-model.cpp
index 9b1444874..275c9ba5e 100644
--- a/src/llama-model.cpp
+++ b/src/llama-model.cpp
@@ -6,6 +6,7 @@
 #include "llama-impl.h"
 #include "llama-mmap.h"
 #include "llama-cparams.h"
+#include "llama-graph.h"
 #include "llama-model-loader.h"
 
 #include "llama-kv-cache.h"
@@ -2121,8 +2122,14 @@ llama_memory_i * llama_model::create_memory(const llama_memory_params & params,
                             filter_attn = [&](uint32_t il) { return il >= hparams.n_layer(); };
                             filter_recr = [&](uint32_t il) { return il >= hparams.n_layer(); };
                         } else {
-                            filter_attn = [&](uint32_t il) { return il < hparams.n_layer(); };
-                            filter_recr = [&](uint32_t il) { return il < hparams.n_layer(); };
+                            const skippy_graph_filter stage_filter = skippy_graph_get_filter();
+                            filter_attn = [&, stage_filter](uint32_t il) {
+                                return il < hparams.n_layer() &&
+                                    (!stage_filter.enabled ||
+                                     (il >= static_cast<uint32_t>(stage_filter.layer_start) &&
+                                      il < static_cast<uint32_t>(stage_filter.layer_end)));
+                            };
+                            filter_recr = filter_attn;
                         }
                     } else if (arch == LLM_ARCH_NEMOTRON_H || arch == LLM_ARCH_NEMOTRON_H_MOE) {
                         filter_attn = [&](uint32_t il) {
diff --git a/src/skippy.cpp b/src/skippy.cpp
index 3016cc915..3c900adde 100644
--- a/src/skippy.cpp
+++ b/src/skippy.cpp
@@ -4433,6 +4433,7 @@ static enum skippy_status skippy_restore_verify_prefix(
                 0,
                 accepted_count,
                 false,
+                SKIPPY_GLM_DSA_PHASE_HINT_VERIFY,
                 out_error);
     } else if (accepted_count > 0) {
         status = skippy_decode_tokens(
@@ -4440,6 +4441,7 @@ static enum skippy_status skippy_restore_verify_prefix(
                 checkpoint->token_ids.data(),
                 accepted_count,
                 false,
+                SKIPPY_GLM_DSA_PHASE_HINT_VERIFY,
                 out_error);
     }
     if (status == SKIPPY_STATUS_OK) {
@@ -6979,8 +6981,6 @@ enum skippy_status skippy_trim_session(
             skippy_set_error(out_error, SKIPPY_STATUS_UNSUPPORTED, "runtime memory type is not supported for trim");
             return SKIPPY_STATUS_UNSUPPORTED;
         }
-    }
-        }
     }
     session->n_past = static_cast<int32_t>(token_count);
     if (session->token_history.size() > token_count) {
-- 
2.54.0

